export interface FormData {
  name: string;
}

export interface Errors {
  name: string;
}

export interface Payload {
  name: string;
}

export interface Category {
  id: number;
  name: string;
  created_at: string;
}